# LCD---LED-Switch-Arduino-
This is a simple C++ project for A LCD - LED Switch - the LCD display will print (LED IS ON)  every time the LED lights are on and the opposite (LED IS OFF) every time the lights are off
